const defaultAudioData = {
  volume: 0.3,
  intensity: 0.5,
  mode: 'I'
};

const tempoBounds = {
  min: 6,
  max: 400
};

const volumeBounds = {
  min: 0,
  max: 127
};

export default defaultAudioData;
export { tempoBounds, volumeBounds };
